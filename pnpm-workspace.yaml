# We publish the @neuralfog scope ourselves, so exempt it from the
# minimum-release-age supply-chain cooldown — lets us install our own
# just-published dev builds without waiting out the policy window.
# Scope glob covers every package + version (no per-release upkeep).
minimumReleaseAgeExclude:
  - '@neuralfog/*'
